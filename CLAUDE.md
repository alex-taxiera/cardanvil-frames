# CLAUDE.md

The project guide for coding agents lives in `AGENTS.md`. Claude Code does not read `AGENTS.md`
natively, so this file imports it — keep instructions in `AGENTS.md`, not here.

@AGENTS.md
